let duckObjet= new Duck(900,300); // you should call the fuction with respect to the height and the width of the page

duckObjet = duckObjet.intalizeDuck();


let containerDiv= document.querySelector('.container');

containerDiv.appendChild(duckObjet);
